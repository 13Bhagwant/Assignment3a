package com.zybooks.pizzaparty.viewmodel;

public class PizzaPartyViewModel {

    //Observables - 3

    //Logic - calculating pizzas

    //Determine number of people at the party
    //Determine level of hunger
    //Calculate number of pizzas required
    // Update UI as appropriate - Using observables
    //.get () -> returns the value in the observables based on the type provided declaration
     //.set(<T>) -> update the value in the observable. Accepts an argument of the type provided at declaration

}
